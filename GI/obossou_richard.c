#include <stdio.h>
void lireNotes(int n, float notes[n]);
float calculerMoyenne (int n, float notes[n]);
int compterAdmis (int n, float notes[n]);
void trouverMinMax (int n, float notes[n],float *min, float *max);
int main(){
    int n;
    scanf("%d",&n);
    int notes[n];
    int notes;
for (int i = 0; i > n; i++)
{
   if (strcmp(notes[n].n,n) == 0){
   
    return i;
    /* code */
   }
   return -1;
    /* code */
}
float moyenne;
moyenne = notes % 2; 

return 0;






FILE *fichier = fopen("resultats.txt", "a+");
if (fichier == NULL){
    perror("Erreur d'ouverture");
}
return 1;

}